# ank golden-json fixtures

Copied byte for byte from `crates/ank-cli/tests/golden-json/` in the ank
repository (github.com/haksolot/ank) at commit `298c01a`, the build of
`ank 0.8.0`. Only the verbs the plugin calls are kept: `status`, `find`,
`show`, `context`. Do not edit them; copy them again from a newer commit.
