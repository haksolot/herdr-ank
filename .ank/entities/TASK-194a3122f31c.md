---
id: TASK-194a3122f31c
type: task
slug: le-binaire-et-ses-tests-compilent-et-passent-sur
title: "Le binaire et ses tests compilent et passent sur Windows : TUI sans exec, racine des worktrees, faux binaires portables"
created: 2026-09-25T19:06:39Z
author: haksolot@omarchy
status: open
scope:
  - src/tui.rs
  - src/main.rs
  - src/ank/**
  - src/work/**
  - tests/**
  - Cargo.toml
  - Cargo.lock
  - src/lib.rs
blocked_by: []
done_criteria: |
  Sur Windows le TUI lance `ank tui` comme enfant, attend, et sort avec son code ; sur Unix il garde `exec`. La racine des worktrees vient de `HOME` sur Unix et de `USERPROFILE` sur Windows. Les tests qui écrivent un faux `herdr` ou `ank` n'écrivent plus de script sh : ils utilisent un binaire d'aide compilé par cargo (un `[[bin]]` de test ou `CARGO_BIN_EXE_`), qui enregistre son argv et rejoue une réponse, sur les trois OS ; `PermissionsExt` et tout `std::os::unix` hors cfg disparaissent des tests. Preuve : les trois jobs du CI (fmt, clippy, test) sont verts sur la branche poussée, URL du run dans le log.
criteria_by: creator
verify: [cargo-test, clippy, fmt-check]
method: tdd
schema: 4
version: 2
---

Applique ADR-599b6f424271. Lire d'abord le log de la tâche CI : il liste ce qui
casse sur Windows aujourd'hui. `tests/install.rs` teste `install.sh` avec un
faux `uname` : sur Windows ces tests sont cfg(unix), et `install.ps1` a les
siens dans sa propre tâche.
