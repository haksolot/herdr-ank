---
id: LOG-84bbf1929f93
type: log
title: "readings taken: <user>@<host> is asked of ank itself (ADR-fa8b: the fallback ank computes): the"
created: 2026-09-25T18:04:30Z
author: haksolot@omarchy/ank-1
scope:
  - src/ank/**
  - src/daemon/**
  - tests/daemon.rs
  - tests/ank_client.rs
about: TASK-56c4d26c623f
seq: 6
schema: 4
version: 1
---

 client runs 'ank status --json --repo <repo>' once with ANK_AGENT removed, keeps identity.value up to its first '/', and caches '<that>/herdr-ank' per Client; the daemon builds a Client per corpus per sync, so that is one extra status per corpus per sync. 'ank watch --where' is a client function (ank::events_jsonl) but runs without ANK_AGENT: watch refuses --repo and addresses no corpus, so there is no corpus to ask the identity of, and it reads nothing as anyone. ank::tui_command (TASK-5dcf) stays out of the plugin identity on purpose: the TUI is the human's. The daemon's remove_var(ANK_AGENT) is gone (the client now sets it); work's in src/main.rs is left, harmless, outside this scope. Test limit: the mutation 'probe keeps ANK_AGENT' is caught only when the test environment itself carries ANK_AGENT, as it does in an ank agent pane.
