---
id: LOG-28d637735c65
type: log
title: "readings taken: a claim is 'attributed' when its holder ends with /<name> and an agent of that name"
created: 2026-09-25T17:55:38Z
author: haksolot@omarchy/ank-1
scope:
  - src/notify.rs
  - src/daemon/**
  - tests/notify.rs
  - Cargo.toml
  - Cargo.lock
  - src/lib.rs
  - src/main.rs
about: TASK-b12178dca482
seq: 4
schema: 4
version: 1
---

 has its pane in the corpus (ADR-fa8b: a bare fallback-identity claim is attributed to no single pane, so it never produces 'terminée par <nom>'); a claim is the same claim while (corpus root, task id, holder) is unchanged, so a renewal is not re-told and a fresh claim after a release is; notify.expiring_minutes = 0 switches the expiry rule off (no minute count is below 0); the first sync compares the queue to 0, so a daemon restart may tell a non-empty queue once, as the body accepts; 'done' is read with 'ank find <id>' (status == done) only for a claim that disappeared, from the worktree the pane sat in.
