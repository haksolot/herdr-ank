---
id: LOG-ac1f49288a64
type: log
title: "review, criterion axis: [[actions]] work/workspace/herdr-ank work and [[panes]] pick/popup"
created: 2026-09-25T17:40:32Z
author: haksolot@omarchy/ank-1
scope:
  - src/work/**
  - src/pick.rs
  - herdr-plugin.toml
  - tests/work.rs
about: TASK-c81aba215f4d
seq: 5
schema: 4
version: 1
---

 (herdr-plugin.toml, test the_manifest_declares...); work opens pick via plugin_pane_open (pick_task); pick lists context --json tasks ready && state open (pick::claimable), keyboard filter/move/select/cancel (Picker, run_terminal), writes the id or empty into HERDR_PLUGIN_STATE_DIR/pick (write_selection); chain worktree create --branch task/<short> --base <default_branch|HEAD> -> tab create --cwd <wt> --env ANK_AGENT=<user>@<host>/ank-<short> -> agent start ank-<short> --kind <config> --pane <tab root pane> -> agent prompt 'ank claim <id>' checked call by call on the fake herdr; code-4 clause per the discrepancy above (notify, no worktree removal, tested); no-corpus refusal before any herdr call, naming the searched path and ank init (tested). Mutations watched red: notification removed, identity without /ank-<short>, blocked task listed, kind hardcoded, pane opened before the corpus check. Removed from the diff: popup width/height nobody asked for. Kept beyond the letter: NotObserved notification (the poll must end), Cancelled when the picker closes. Constraint axis (ank context on src/work, src/pick.rs, src/main.rs, herdr-plugin.toml, Cargo.toml, tests/work.rs): ADR-3cd1 held - corpus read only via ank Client (context, status, find), find_corpus only tests that .ank/ exists and opens nothing under it; ADR-357c held - every herdr act through the herdr Client argv; ADR-aca6 held - both manifest entries invoke the herdr-ank binary, no script; ADR-fa8b held - ANK_AGENT=<user>@<host>/<herdr agent name> with the fallback identity ank computes.
