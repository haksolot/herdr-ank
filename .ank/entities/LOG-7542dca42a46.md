---
id: LOG-7542dca42a46
type: log
title: "released: Measured (see log): herdr 0.9.1 does not run [[startup]] on 'plugin enable' (no daemon"
created: 2026-09-25T18:05:52Z
author: haksolot@omarchy/ank-1
scope:
  - src/daemon/**
about: TASK-00931a49cf9e
seq: 3
schema: 4
version: 1
---

 process, empty plugin log), and its docs say startup hooks run only after session restore or an experimental live handoff ('herdr update --handoff'), and are 'one-shot initialization commands rather than supervised daemons' that should exit. The only ways to replay it restart or replace the server that hosts ank-2, ank-3 and this agent, which the task forbids. What is missing: a decision on how the daemon lives - (a) keep [[startup]] and measure after a human restarts herdr at a quiet moment, accepting no supervision and no restart after a crash; (b) a supervisor outside herdr (systemd user unit / launchd) with [[startup]] only ensuring it runs; (c) an event hook or another herdr entry point that herdr does supervise, if 0.9.x has one. ADR-c8e7 assumed [[startup]] hosts a long process; that premise now contradicts herdr's own docs.
