---
id: LOG-466f927e6fc3
type: log
title: "review (second pass, after the rebase on 194a). Criterion axis, clause by clause: manifest"
created: 2026-09-25T19:57:43Z
author: haksolot@omarchy/ank-2
scope:
  - install.ps1
  - herdr-plugin.toml
  - .github/workflows/release.yml
  - tests/install_ps1.rs
  - README.md
  - tests/install.rs
about: TASK-4e8fd40b787a
seq: 11
schema: 4
version: 1
---

 platforms + two [[build]] -> herdr-plugin.toml; .exe clause -> herdr code cited in LOG-0433f1bdd26e, no variants; install.ps1 PS 5.1, TLS 1.2, version from manifest, zip+SHA256SUMS from v<version> or HERDR_ANK_RELEASE_BASE (local dir), Get-FileHash, Expand-Archive to bin\, cargo fallback, exit 1 naming URL and cargo -> install.ps1; release.yml windows-latest msvc .zip, SHA256SUMS over 5 -> release.yml; tests/install_ps1.rs cfg(windows) three cases -> green on the windows job (run above); actionlint 1.7.12 -shellcheck= clean. Added beyond the first pass: explicit Import-Module of Utility/Archive from $PSHOME, required for the Get-FileHash clause to hold under an inherited PSModulePath (measured). Out-of-scope hunk: tests/install.rs, forced by the two [[build]] (discrepancy logged). Constraint axis (ADR-599b, ADR-6fb7) re-read on the touched paths: two build scripts and no more, one binary, every non-build entry still ./bin/herdr-ank, five published targets with the windows one as .zip, version refusal unchanged; nothing touches the daemon.
