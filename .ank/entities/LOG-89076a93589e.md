---
id: LOG-89076a93589e
type: log
title: "self-review: criterion axis — named pipe opened by OpenOptions read+write under cfg(windows),"
created: 2026-09-25T19:18:12Z
author: haksolot@omarchy/ank-1
scope:
  - src/herdr/**
  - tests/herdr_client.rs
about: TASK-7868b3db8404
seq: 5
schema: 4
version: 1
---

 UnixStream under cfg(unix) (events.rs Connection); stream behind a Read+Write type chosen by cfg, and subscribe_over<S: Read+Write> carries the NDJSON dialogue; std::os::unix in src/herdr only at events.rs:161,171, both on cfg(unix) items; NDJSON decoding tests replay in memory on every OS; the socket test and the FakeHerdr (sh) CLI tests are cfg(unix); a cfg(windows) test covers the absent pipe, error naming the path. Unanswered: the Windows CI proof (discrepancy above). Hunk beyond the letter: FakeHerdr tests gated cfg(unix), needed for the file to be green on windows at all; TASK-194a replaces them with a portable helper. Constraint axis — ank context src/herdr/events.rs: ADR-599b (named pipe as file, same NDJSON, cfg(unix)/cfg(windows) each with its counterpart) holds; ADR-357c (socket for events.subscribe only, unknown fields ignored) unchanged; ADR-3cd1, ADR-fa8b not touched. Nothing broken.
