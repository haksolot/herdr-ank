---
id: TASK-3513e2275e4b
type: task
slug: work-passe-agent-args-herdr-agent-start
title: work passe agent.args à herdr agent start
created: 2026-09-25T17:57:21Z
author: haksolot@omarchy/ank-1
status: open
scope:
  - src/work/**
  - src/herdr/**
  - tests/work.rs
  - tests/herdr_client.rs
  - Cargo.toml
  - Cargo.lock
  - src/lib.rs
  - src/main.rs
blocked_by: []
done_criteria: |
  Le `Client` herdr expose `agent_start(name, kind, pane, args)` qui ajoute `-- <arg>...` à `herdr agent start <name> --kind <kind> --pane <pane>` quand `args` n est pas vide, et rien sinon. `herdr-ank work` passe `config.agent.args`. Les tests vérifient l argv exact sur le binaire factice avec et sans arguments, et le README ne dit plus « not passed yet » pour `agent.args`.
criteria_by: creator
verify: [cargo-test, clippy, fmt-check]
method: tdd
schema: 4
version: 1
---

Découvert en écrivant le README (TASK-e468) : `agent.args` est documentée et lue par la config (TASK-90fb), mais `work` (TASK-c81a) ne la transmet pas, et le client herdr n a pas de paramètre pour elle. herdr 0.9.1 : `herdr agent start <NAME> --kind <KIND> --pane <ID> [OPTIONS] [-- [AGENT_ARG]...]`.
